/**
 * ESTE ARQUIVO É GERADO AUTOMATICAMENTE PELO OPENAPI GENERATOR
 * 
 * Este é um arquivo de exemplo que simula a estrutura gerada.
 * Quando você rodar o comando:
 * 
 *   openapi-generator-cli generate -i http://localhost:8080/v3/api-docs -g typescript-fetch -o src/api
 * 
 * Este arquivo será substituído pela versão real gerada a partir do seu Swagger.
 */

// Re-exporta todas as APIs
export * from './apis';

// Re-exporta todos os modelos
export * from './models';

// Re-exporta o runtime (Configuration, etc.)
export * from './runtime';
